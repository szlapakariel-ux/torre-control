# Orden Torre — sin orden activa

No hay orden activa en este momento.

Para iniciar un ciclo, la Torre debe reemplazar este archivo con una orden basada en `.torre/templates/orden_template.md`. La orden debe incluir los cuatro campos obligatorios: `PROYECTO_FUNCIONAL`, `REPO_TECNICO`, `RAMA_OBJETIVO`, `EJECUTOR` (ver `protocolo.md`, secciones "Identidad de proyecto" y "Control de concurrencia").

Operadores IA: si lees este placeholder, **no actuar**. No hay nada que ejecutar.
